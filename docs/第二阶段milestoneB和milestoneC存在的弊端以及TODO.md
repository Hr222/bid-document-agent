# TODO

本文档用于记录当前阶段已经识别出来、但暂时不在本轮立刻解决的待办事项，避免后续推进时遗忘。

## OCR 相关待办

- `TODO`：补一轮面向制度文档录入场景的 OCR 方案梳理与落地计划。
- 当前背景：现有很多制度文档并不是原生可直接解析文本，而是扫描件、图片版 PDF 或 OCR 质量不稳定的文件。
- 当前问题：这会直接导致制度录入成本较高，人工处理较多，知识库扩充速度受影响。
- 当前结论：OCR 仍然重要，但暂时不作为本轮最优先推进项；后续在有更明确方案后，再单独展开。
- 后续再看时建议优先回答：
  - 需要先解决的是“能不能录入”，还是“录入后质量是否稳定”。
  - 是先做最小可用 OCR 流程，还是先挑制度样本做针对性验证。
  - OCR 问题应作为知识获取侧专项推进，还是并入后续更完整的入库工程化阶段。

## 检索评测与样本相关待办

- `TODO`：补齐 Milestone B / Milestone C 的检索评测样本，形成比当前最小评测集更稳定的验收资产。
- 当前背景：现有 `baseline / benchmark` 已经能跑通，也能比较 `exact / hnsw` 的命中与耗时，但当前评测集规模仍然过小。
- 当前问题：样本不足会导致很多结论只能作为“方向正确”的信号，而很难作为“已经足够稳定”的阶段性证据。
- 当前结论：后续如果继续做检索优化、rerank 调整、问答链路改造或召回策略扩展，评测样本建设会成为优先级很高的基础工作。
- 当前已暴露出的短板包括：
  - 制度类别覆盖还不够，当前样本偏集中，难以说明跨制度类型的一致性。
  - 问题类型覆盖还不够，像定义类、适用范围类、时效类、流程类、条件类问题都需要更均衡的样本。
  - 目前对“命中正确但排序不理想”和“完全未命中”的区分还不够细，后续可考虑补更明确的评测标签。
  - 现在的 benchmark 更像最小验证集，还不足以支撑更稳定的性能趋势判断。
- 后续再看时建议优先回答：
  - 是先把现有制度样本扩到足够多，还是先补齐不同问题类型的代表性 case。
  - 评测集是否要区分 `retrieval baseline`、`rerank 验收`、`ask 链路验收` 三类资产，避免混用。
  - 是否需要沉淀一份可长期复用的“标准验收问题集”，用于后续每个 milestone 的回归验证。
