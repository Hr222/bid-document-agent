# TODO

本文档用于记录 `Milestone B / C` 暴露出的弊端、当前收口动作，以及仍需继续推进的剩余事项。

## OCR 相关问题

- 当前收口动作：
  - 已在入库主链路中保留 `direct -> OCR -> ingest_guard` 的稳定处理路径。
  - 已补 OCR 基础单元测试，覆盖扫描 PDF 目标选择、OCR 禁用时的返回、OCR 成功后的元数据清理。
  - 已放开 `.doc`、图片文件、图片式 DOCX 的最小入口支持，减少真实制度样本卡在准入阶段的情况。
  - 已新增 `docs/制度文档OCR验收与落地计划.md`，明确当前验收口径与后续推进顺序。
- 当前背景：现有很多制度文档并不是原生可直接解析文本，而是扫描件、图片版 PDF 或 OCR 质量不稳定的文件。
- 当前问题：这会直接导致制度录入成本较高，人工处理较多，知识库扩充速度受影响。
- 当前结论：`最小可用 OCR 流程` 已经具备，当前主要矛盾已从“有没有 OCR”切换到“样本验收是否稳定、是否值得继续扩批量”。
- 剩余待办：
  - 补一组真实扫描件样本，形成可重复跑的 OCR 验收集。
  - 在实际样本上记录“可直接入库 / 需要人工复核 / 不建议入库”三类结果。
  - 结合知识获取节奏，再决定是否把 OCR 扩展为单独专项工程。

## 检索评测与样本相关问题

- 当前收口动作：
  - 已把 `tests/retrieval_eval_set_step_a.json` 从 2 条样本扩到 9 条样本。
  - 已为评测样本补充 `policy_category / question_type / tags / notes` 元数据。
  - 已在 `app/scripts/run_retrieval_baseline.py` 中增加评测集结构校验、重复 `case_id` 拦截和覆盖摘要输出。
- 当前背景：现有 `baseline / benchmark` 已经能跑通，也能比较 `exact / hnsw` 的命中与耗时，但当前评测集规模仍然过小。
- 当前问题：虽然问题类型覆盖已明显好于最初版本，但制度类别仍集中在现有人事制度样本上，对跨资料类型的一致性判断仍然有限。
- 当前结论：`问题类型覆盖不足` 已经得到第一轮缓解；`跨制度类别覆盖不足` 仍依赖后续更多已入库样本。
- 当前剩余短板：
  - 资料类别仍偏单一，后续要补管理制度之外的其他制度或规范资料。
  - 目前 benchmark 仍以 retrieval 主链路为主，`rerank` 和 `ask` 还应各自沉淀更专门的验收集。
  - 现在已有“top1/top3/zero-hit”维度，但仍可继续细化“命中正确但排序不理想”的标签。
- 后续再看时建议优先回答：
  - 下一批样本应优先扩制度类别，还是继续细化问题类型。
  - 是否把 `retrieval baseline`、`rerank 验收`、`ask 链路验收` 彻底拆成三份独立资产。
  - 是否需要在 benchmark 输出中加入更强的失败标签，方便长期回归观察。
